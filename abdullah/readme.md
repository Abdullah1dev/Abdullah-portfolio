# Abdullah Portfolio (Backend-Focused Django Project)

This is a personal portfolio project built with Django.  
The main goal is to build backend features like authentication, admin dashboards, project CRUD, and APIs to gain strong backend experience.

## Setup

1. Clone the repository
2. Create a virtual environment and activate it
3. Install requirements  
   ```bash
   pip install -r requirements.txt
